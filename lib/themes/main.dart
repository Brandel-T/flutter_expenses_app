import 'package:flutter/material.dart';

// ********** GENERAL **********
const horizontalOffset = 20.0;
const verticalOffset = 16.0;

// ********** TRANSACTION ITEM ********
const transactionHeight = 120.0;
const boxShadow = BoxShadow(color: Colors.black12, blurRadius: 10, offset: Offset(0, 8));
const transactionRadius = 23.0;

// ********** HOME SCREEN **************
const homePrimaryDark = Color(0xff2E2F74);
const homePrimaryLight = Color(0xff05059B);

const homeHeaderBannerRadius = 36.0;
const homeHeaderBannerTextColor = Color(0xffD1E2FF);

const lastTransactionContainerBG = Color(0xffD1E2FF);
const lastTransactionAmountColor = Color(0xff0F9E0D);
const lastTransactionNameColor = Color(0xff073B8B);
const lastTransactionRadius = 23.0;

const homeOtherTransactionsRadius = 23.0;
const homeOtherTransactionsShadow = BoxShadow(
  color: Colors.black12,
  blurRadius: 10,
  offset: Offset(0, 8),
);


